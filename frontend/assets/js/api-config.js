/*
 * ASK BAGBIN EDUCATION FUND - API CONFIGURATION
 *
 * LOCAL DEVELOPMENT:
 *   Frontend on Live Server (5500), backend on Node (5000):
 *   http://localhost:5000/api
 *
 * PRODUCTION / CPANEL:
 *   If the frontend and API use the same domain, change to:
 *   /api
 *
 * If your API uses another domain, put the full API base URL here.
 */
window.ASK_API_BASE = window.ASK_API_BASE || 'http://localhost:5000/api';
